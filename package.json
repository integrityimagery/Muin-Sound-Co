{
  "name": "muin-sound-co",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "contrast": "node scripts/contrast-check.mjs",
    "audit": "node --experimental-strip-types --no-warnings scripts/audit.mjs",
    "test:a11y": "node scripts/a11y-test.mjs",
    "verify": "npm run contrast && npm run check && npm run build && npm run audit",
    "audio": "python3 scripts/generate-placeholder-audio.py"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.1.0",
    "@fontsource/alegreya-sans": "^5.1.0",
    "astro": "^7.2.9",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "playwright": "^1.62.1",
    "typescript": "^6.0.3"
  }
}
